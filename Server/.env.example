### Copy this file to `.env` and fill real values. Do NOT commit credentials.
# Example MongoDB connection string (replace placeholders):
# MONGO_URI=mongodb+srv://<DB_USER>:<DB_PASSWORD>@cluster0.mongodb.net/<DB_NAME>?retryWrites=true&w=majority
# Port the server should listen on
PORT=3000

# Admin credentials for the simple login (use strong password in production)
# ADMIN_USER=admin
# ADMIN_PASS=change_this_password

# Secret to sign JWT tokens
# JWT_SECRET=your_jwt_secret_here

# MONGO_URI=mongodb+srv://<DB_USER>:<DB_PASSWORD>@cluster0.mongodb.net/<DB_NAME>?retryWrites=true&w=majority
